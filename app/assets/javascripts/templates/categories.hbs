<div class="sidebar pure-u-1 pure-u-sm-4-24">
  <ul>
    {{#each category in controller}}
      {{#link-to 'category' category tagName="li"}}
        {{category.name}}
      {{/link-to}}
    {{/each}}
  </ul>
</div>

<div class="content pure-u-1">
  {{outlet}}
</div>
