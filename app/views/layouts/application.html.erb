<!DOCTYPE html>
<html>
<head>
  <title>Letnar</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Adresář služeb na Letné a v Holešovicích">
  

  <%= stylesheet_link_tag 'application', media: 'all' %>

  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="header">
    <div class="container pure-g">
      <div class="pure-u-sm-20-24">
        <h1>
          <a href="/">LETNÁŘ</a>
          <span>Adresář služeb na Letné a v Holešovicích</span>
        </h1>
      </div>

      <div class="pure-u-sm-4-24">
        <div class="header-navigation">
          <% if current_user.present? %>
            </i><i class="fa fa-user"></i><%= current_user.name %>
          <% else %>
            <i class="fa fa-user"></i><%= link_to "Login with Facebook", "auth/facebook" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>

  <div class="pure-u-1">
    <div class="container">

      <%= yield %>

      <div id="ember-app"></div>

    </div>
  </div>
</div>

</body>
</html>
